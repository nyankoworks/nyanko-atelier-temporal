# ── 非公開ルール ──
# フォルダ名・ファイル名の先頭に「_」を付けたものは、GitHub（ネット）に上げません。
#   → サイトにも出ず、自分のPCの中だけに残ります（完全非公開）
_*
**/_*

# ただし build 自動公開のしくみ（_site）は上げてOKなので、下書き規則から除外はしない
# （_site は Actions 側で毎回作り直すため、そもそも push 不要）
_site/

# OSの一時ファイル
.DS_Store
Thumbs.db
