# CLAUDE.md — このプロジェクトの進め方ルール

AI（Claude Code / Cowork）がこのフォルダで作業するときに、最初に読むメモです。
（＝毎回説明しなくても“いつもの品質”で動いてもらうための1枚）

## このプロジェクトは何？
「にゃんこの時空アトリエ」＝ note.com風のポートフォリオサイト。
**works/ にフォルダを1つ置くだけ**でカードが自動追加される仕組み（`build.py`）。

## いちばん大事なルール
- **公開/非公開**：フォルダ・ファイル名の頭に `_` を付けたら**完全非公開**（サイトにもGitHubにも出ない）。個人メモ・下書き・素材はここへ。
- **カードの種類**は中身で自動判定：`link.txt`=外部リンク / `index.html`=自作ページ / 画像=ギャラリー / PDF=PDF。
- **並び順**＝フォルダ名の先頭の数字（大きいほど上）。
- 各作品ページには**共通の「一覧に戻る」ボタン**がビルド時に自動で入る。

## 更新の仕方
- `更新.bat`（Windows）をダブルクリック → `build.py` が走って `_site/` を作り、ページが開く。
- GitHubに push すると GitHub Actions が自動でビルド＆公開。

## 計測・SEO（config.json で設定）
- `ga4_id`（GA4）/ `clarity_id`（ヒートマップ）/ `gtm_id`（タグマネージャ）/ `site_url` / `google_site_verification`。
- 全ページに自動注入。カードのクリックは GA4 イベント `select_card` で計測。
- `site_url` を入れると sitemap.xml / robots.txt / llms.txt も自動生成。

## 相手（依頼者）の好み
- 実行の前に一度確認する。ステップは小分け。専門用語はかみ砕いて、日本語で。
- まず「たたき台 → 修正」。コピペで使える形が好き。数字は盛らない（誠実に）。

## 触ってはいけない/注意
- `_` 付き（`_メモ/` `_AIノート/` など）は非公開。公開物に混ぜない。
- クライアント名・実データを公開ページに出すときは必ず許可を確認。
